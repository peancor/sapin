import { renderTimedQuizManifest } from './manifest';
import type { BuiltinToolPackage } from '../types';

export const renderTimedQuizPackage: BuiltinToolPackage = {
	manifest: renderTimedQuizManifest
};

export { renderTimedQuizManifest } from './manifest';

