import { renderExecutiveFlexibilityTestManifest } from './manifest';
import type { BuiltinToolPackage } from '../types';

export const renderExecutiveFlexibilityTestPackage: BuiltinToolPackage = {
	manifest: renderExecutiveFlexibilityTestManifest
};

export { renderExecutiveFlexibilityTestManifest } from './manifest';
